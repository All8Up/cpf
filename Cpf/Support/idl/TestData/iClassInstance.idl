module cpf::plugin;
import types from cpf;
import iUnknown from cpf::gom;

interface iRegistry;

interface iClassInstance : iUnknown
{
	result CreateInstance([in] iRegistry* registry, [in] iUnknown* outer, [out] iUnknown** outIface);
}
